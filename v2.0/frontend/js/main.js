import{api}from"./api.js";import{analyzeEvents}from"./detections.js";import{normalizeEvent,parseLogs}from"./parsers.js";import{bindPhase6,downloadReport,renderPhase6}from"./phase6.js";import{testRule}from"./rule_engine.js";import{createStore}from"./state.js";import{bindUi,render,renderBackendStatus,renderRuleTest}from"./ui.js";
const store=createStore(),renderAll=s=>{render(s);renderPhase6(s)};store.subscribe(renderAll);renderAll(store.get());
async function analyze(file,text){let rawEvents,sourceName;if(file?.name.toLowerCase().endsWith(".evtx")){if(!store.get().backendOnline)throw new Error("EVTX requires the local v2 service.");const p=await api.importEvtx(file);rawEvents=p.events;sourceName=p.sourceName}else if(file){rawEvents=parseLogs(await file.text());sourceName=file.name}else{rawEvents=parseLogs(text);sourceName="Pasted logs"}const analysis={...analyzeEvents(rawEvents,store.get().rules,store.get().detectionSettings),sourceName};store.set(analysis);if(store.get().backendOnline){try{await api.saveAnalysis(analysis);store.set({attackHeatmap:(await api.attackHeatmap(analysis.alerts)).techniques})}catch(e){console.warn(e.message)}}}
async function refreshRules(){const[r,s]=await Promise.all([api.listRules(),api.getDetectionSettings()]);store.set({rules:r.rules,detectionSettings:s})}async function refreshIncidents(status=store.get().incidentFilter){if(!store.get().backendOnline)return;const p=await api.listIncidents(status);store.set({incidents:p.incidents,incidentFilter:status,incidentError:""})}async function refreshPhase6(){const[h,i]=await Promise.all([api.listHunts(),api.listIocs()]);store.set({hunts:h.hunts,huntRuns:h.runs,iocs:i.iocs})}
const saveRule=async r=>{await api.saveRule(r);await refreshRules()},toggleRule=async(id,e)=>{await api.toggleRule(id,e);await refreshRules()},deleteRule=async id=>{await api.deleteRule(id);await refreshRules()},saveSettings=async s=>store.set({detectionSettings:await api.saveDetectionSettings(s)});async function saveRuleOverride(id,severity,threshold){const c=store.get().detectionSettings,severityOverrides={...(c.severityOverrides||{}),[id]:severity},thresholdOverrides={...(c.thresholdOverrides||{})};if(threshold)thresholdOverrides[id]=threshold;else delete thresholdOverrides[id];await saveSettings({...c,severityOverrides,thresholdOverrides})}async function importRules(rs){for(const r of rs)await api.saveRule(r);await refreshRules()}
async function createIncidentFromAlert(id){const s=store.get(),a=s.alerts.find(x=>x.id===id);if(!a)throw new Error("That alert is no longer available.");const i=await api.createIncident({sourceName:s.sourceName,riskScore:s.riskScore,alert:{...a,riskScore:a.riskScore??s.riskScore,events:(a.events||[]).slice(0,20)}});store.set({incidents:(await api.listIncidents(s.incidentFilter)).incidents,selectedIncident:i})}async function createCorrelatedIncident(){const s=store.get();if(!s.alerts.length)throw new Error("Analyze logs first.");const primary=[...s.alerts].sort((a,b)=>({High:3,Medium:2,Low:1}[b.severity]-({High:3,Medium:2,Low:1}[a.severity]))[0],i=await api.createIncident({title:`Correlated activity: ${s.alerts.length} security alerts`,sourceName:s.sourceName,riskScore:s.riskScore,severity:s.riskLevel,alerts:s.alerts,alert:primary,evidence:s.events.slice(0,200)});store.set({incidents:(await api.listIncidents()).incidents,selectedIncident:i})}
async function selectIncident(id){const[i,t]=await Promise.all([api.getIncident(id),api.getInvestigationTimeline(id)]);store.set({selectedIncident:i,investigationTimeline:t.timeline,incidentError:""})}async function updateIncident(id,p){store.set({selectedIncident:await api.updateIncident(id,p),incidents:(await api.listIncidents(store.get().incidentFilter)).incidents})}async function addIncidentNote(id,n){store.set({selectedIncident:await api.addIncidentNote(id,n),incidents:(await api.listIncidents(store.get().incidentFilter)).incidents})}async function generateIncidentSummary(id){store.set({summaryGenerating:true});try{await api.generateIncidentSummary(id);store.set({selectedIncident:await api.getIncident(id),summaryGenerating:false})}catch(e){store.set({summaryGenerating:false,incidentError:e.message});throw e}}function testSelectedRule(id,text){const r=store.get().rules.find(x=>x.id===id);if(!r)throw new Error("Choose a detection rule.");renderRuleTest(testRule(r,parseLogs(text).map(normalizeEvent),store.get().detectionSettings))}
async function runHunt(id){const s=store.get();if(!s.events.length)throw new Error("Analyze logs before running a threat hunt.");store.set({huntResult:await api.runHunt(id,s.events,s.sourceName),phase6Error:""})}async function saveIoc(i){await api.saveIoc(i);store.set({iocs:(await api.listIocs()).iocs})}async function deleteIoc(id){await api.deleteIoc(id);store.set({iocs:(await api.listIocs()).iocs})}async function matchIocs(){const s=store.get();if(!s.events.length)throw new Error("Analyze logs before matching IOCs.");store.set({iocResult:await api.matchIocs(s.events)})}async function importSigma(t){await api.importSigma(t);await refreshRules()}async function exportIncidentReport(id,f){downloadReport(await api.createIncidentReport(id,f))}
bindUi(store,{analyze,saveRule,toggleRule,deleteRule,saveSettings,saveRuleOverride,importRules,refreshIncidents,createIncidentFromAlert,createCorrelatedIncident,selectIncident,updateIncident,addIncidentNote,generateIncidentSummary,exportIncidentReport,testSelectedRule});bindPhase6(store,{runHunt,saveIoc,deleteIoc,matchIocs,importSigma,exportIncidentReport});try{const s=await api.status();store.set({backendOnline:true,aiSummary:s.aiSummary||store.get().aiSummary});renderBackendStatus(true,s.version);await Promise.all([refreshRules(),refreshIncidents(),refreshPhase6()])}catch{renderBackendStatus(false)}
